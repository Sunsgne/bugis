! ===== GENERIC EVPN-VPWS (point-to-point) intent =====
! circuit={{ circuit.code }} device={{ device.name }} vni={{ circuit.vni }}
! local-ac={{ endpoint.interface_name }} vlan={{ endpoint.vlan_id or circuit.vlan_id }}
! rd={{ circuit.route_distinguisher }} rt={{ circuit.route_target }} esi={{ circuit.esi }}
! NOTE: generic placeholder - vendor template not found for {{ vendor }}
