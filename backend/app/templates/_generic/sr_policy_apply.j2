! SR explicit path intent · circuit={{ circuit.code }} device={{ device.name }}
! path_mode={{ path_mode }} segments={{ path_segments | join(' -> ') }}
! NOTE: generic placeholder - vendor template not found for {{ vendor }}
