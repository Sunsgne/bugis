# Web framework
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# Data / ORM
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2
# PostgreSQL driver (used in docker-compose; SQLite needs no driver).
psycopg[binary]==3.3.4

# Migrations
alembic==1.18.4

# Templating (vendor config rendering)
Jinja2==3.1.6

# Auth & security
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
pyotp==2.10.0
httpx==0.28.1

# Telemetry / SNMP (IF-MIB walks)
pyasn1>=0.6.1
pyasn1-modules>=0.4.2
pysnmp==7.1.27

# Metrics
prometheus-client==0.25.0

# Optional Redis cache (portal / dashboard hot reads)
redis==5.2.1

# Southbound device drivers (SSH CLI / NETCONF) — required when dry_run=false
netmiko>=4.7.0
ncclient>=0.7.1

# Testing
pytest==9.1.0
