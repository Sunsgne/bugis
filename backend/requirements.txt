# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Data / ORM
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
# PostgreSQL driver (used in docker-compose; SQLite needs no driver).
psycopg[binary]==3.2.3

# Migrations
alembic==1.14.0

# Templating (vendor config rendering)
Jinja2==3.1.5

# Auth & security
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt>=4.1; pin to 4.0.1.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
pyotp==2.9.0
httpx==0.28.1

# Telemetry / SNMP (IF-MIB walks) — PySNMP 6.x (Python 3.12+)
pyasn1>=0.5.1
pyasn1-modules>=0.3.0
pysnmp>=6.2.0,<7.0.0

# Metrics
prometheus-client==0.21.1

# Southbound device drivers (SSH CLI / NETCONF) — required when dry_run=false
netmiko>=4.5.0,<5.0.0
ncclient>=0.6.16,<0.7.0

# Testing
pytest==8.3.4
