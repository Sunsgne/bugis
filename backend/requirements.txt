# Web framework
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.22

# Data / ORM
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.1
# PostgreSQL driver (used in docker-compose; SQLite needs no driver).
psycopg[binary]==3.3.4

# Migrations
alembic==1.18.4

# Templating (vendor config rendering)
Jinja2==3.1.6

# Auth & security
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt>=4.1; pin to 4.0.1.
bcrypt==4.0.1
python-jose[cryptography]==3.5.0
pyotp==2.10.0
httpx==0.28.1

# Telemetry / SNMP (IF-MIB walks)
pyasn1>=0.6.1
pyasn1-modules>=0.4.2
pysnmp>=6.2.6,<7.0.0

# Metrics
prometheus-client==0.25.0

# Southbound device drivers (SSH CLI / NETCONF) — required when dry_run=false
netmiko>=4.7.0,<5.0.0
ncclient>=0.7.1,<0.8.0

# Testing
pytest==9.1.0
